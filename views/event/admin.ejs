<table class="table table-striped table-hover">
    <tbody>
    <% events.forEach( function(model) { %>
        <tr>
            <td><%= model.id %></td>
            <td><a href="/event/update/<%= model.id %>"><%= model.name %></a></td>
        </tr>
    <% }); %>
</tbody>
</table>
